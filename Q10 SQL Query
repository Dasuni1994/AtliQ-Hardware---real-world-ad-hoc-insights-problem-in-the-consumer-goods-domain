Q10. Get the Top 3 products in each division that have a high total_sold_quantity in the fiscal_year 2021? 
The final output contains these fields,

division
product_code
product
total_sold_quantity
rank_order




with cte01 as (
select division, s.product_code as product_code ,product , sold_quantity ,variant from fact_sales_monthly s
join dim_product p 
on s.product_code = p.product_code
where s.fiscal_year = "2021"
) ,
cte02 as (
select division, product_code ,concat(product ," - " ,variant) as product , sum(sold_quantity) as total_sold_quantity ,
dense_rank() over (partition by division order by  sum(sold_quantity) desc) as rank_order from cte01
group by division ,product_code,product,variant )

select * from cte02 where rank_order <= 3;
