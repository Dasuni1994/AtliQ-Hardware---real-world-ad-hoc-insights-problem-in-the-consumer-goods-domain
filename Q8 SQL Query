Q8. In which quarter of 2020 did the maximum total_sold_quantity occur? The final output contains these fields sorted by the 

total_sold_quantity,
Quarter
total_sold_quantity



with cte01 as (SELECT * , month(date) as month_num FROM gdb023.fact_sales_monthly where fiscal_year = 2020),

cte02 as (
select *, case 
				when month_num in (9,10,11) then "Q1"
				when month_num in (12,1,2) then "Q2" 
                when month_num in (3,4,5) then "Q3" 
                else "Q4"
                end as Quarter from cte01)
                
                
select Quarter , sum(sold_quantity)  as total_sold_quantity from cte02 group by Quarter ;


